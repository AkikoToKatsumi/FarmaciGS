# ⚙️ VARIABLES DE ENTORNO - BACKEND
# Copia este archivo como .env y configura los valores reales

# ===== BASE DE DATOS =====
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Farmaciadb
DB_USER=postgres
DB_PASSWORD=CAMBIAR_ESTO_POR_PASSWORD_SEGURO

# ===== SERVIDOR =====
PORT=4004
NODE_ENV=production

# ===== JWT (Autenticación) =====
# Genera un secreto seguro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CAMBIAR_ESTO_POR_UN_SECRETO_MUY_LARGO_Y_ALEATORIO
JWT_EXPIRES_IN=7d

# ===== CORS =====
# Cambia por tu dominio real
CORS_ORIGIN=https://tudominio.com

# ===== API =====
API_URL=https://tudominio.com/api

# ===== NOTAS =====
# 1. NUNCA subas este archivo a Git con valores reales
# 2. Cambia TODOS los valores que dicen "CAMBIAR_ESTO"
# 3. Usa contraseñas fuertes
# 4. El JWT_SECRET debe ser único y aleatorio
